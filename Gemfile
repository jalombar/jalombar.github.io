# Only needed for optional local preview (bundle exec jekyll serve).
# GitHub Pages builds the site on its own and ignores this file.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
